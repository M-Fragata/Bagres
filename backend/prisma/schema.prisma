generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Schedules {
  id        String   @id @default(uuid())
  atleta    String
  date      String
  hour      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("Schedules")
}

model Atletas {
  id        String   @id @default(uuid())
  firstName String
  lastName  String
  email     String   @unique
  password  String
  role      String @default("user")
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt

  @@map("Atletas")
}

model Config {
  id        Int      @id @default(1)
  dias      String[] // Ex: ["Segunda", "Terça"]
  horarios  String[] // Ex: ["08:00", "08:45"]
  updatedAt DateTime @updatedAt
}